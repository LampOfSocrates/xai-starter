# Wrap-up — 13 Jun 2026, 14:50

## Project
`C:\Users\soura\code\2026\xai-starter` — a hands-on protein-ML lesson suite (pLM,
GNN, IG tracks) that doubles as the lab bench for three MSc-scale xAI research
projects. The research vault (source of truth for the science) is at
`G:\My Drive\ObsidianDB\vault\obsidian\xAI for DL in Proteins\00_xAI_for_DL_in_Proteins_Definitive_Guide.md`.
Today's work reorganised the single `ig/` "Idea 4" track into **three parallel
research tracks over a shared `common/` package**, then built two of them out.

## Today's progress
- **Retired the "Idea 4"/`idea4` vocabulary** across the repo → cluster-ACRONYM names
  (`attrib-PINCH`, `contact-CLASP`, `attrib-EDITS`, fallback `attrib-FAITH`).
- **Migrated `ig/` → `pinch/`** (history preserved via `git mv`): `idea4_common.py`
  → `pinch_common.py`, `ig_l3..l7` → `pinch_l1..l5` (committed outputs preserved),
  runner/reporter/builder/smoke renamed, MLflow experiment `idea4-ppi-xai` →
  `pinch-ppi-xai`. Commit `d28fd2c`.
- **Created `common/`** package (config, structures, graphs, metrics + `__init__`)
  by lifting the shared bits out of the old combined module; moved the two IG
  primer notebooks (`ig_l1`, `ig_l2`) here. Each track imports `common`.
- **Built `contact-CLASP` (`clasp/`)** as a runnable miniature (BPTI `2ptc:I`):
  `clasp_common.py` (ESM-2 attention contacts+APC, real PDB Cα ground truth,
  mean-field DCA on a contact-seeded simulated MSA, Neff/L, gradient/IG contact
  map, rank-fusion, precision@L) + 5 executed notebooks + smoke + builder.
  Reproduces the headline axis: DCA P@L 0.79→0.41 as Neff/L falls; attention flat.
- **Built `attrib-EDITS` (`edits/`)** as a runnable miniature (GB1 domain):
  `edits_common.py` (ESM-2 zero-shot fitness predictor, greedy counterfactual with
  on-manifold plausibility penalty, gradient saliency) + 5 executed notebooks +
  smoke + builder. Off-manifold flips in 2 implausible edits; on-manifold takes 6
  realistic ones. `validate_against_ddg()` **raises by design** (leakage gate).
- **`CLAUDE.md` in all three track folders**: shared "leakage lives in the borrowed
  model's training set" rule + per-track specifics. `edits/CLAUDE.md` has the full
  ThermoMPNN×Megascale (severe) vs ESM-1v (mild) protocol and the open item
  (inspect ThermoMPNN `dataset_splits/`).
- **Quarto site**: wired `common/`, `pinch/`, `clasp/`, `edits/` into `_quarto.yml`
  (render globs + sidebar sections); `quarto render` → **45 pages, no errors**.
- Both track builds committed + pushed: commit `90456ce`. All three smoke tests
  green (pinch ~9s, clasp ~8s, edits ~4s).

## File inventory (current state)
| Path | Purpose | Status |
|---|---|---|
| `common/{config,structures,graphs,metrics,__init__}.py` | shared engine for all tracks | new, verified |
| `common/ig_l1_simple.ipynb`, `ig_l2_tiny_network.ipynb` | IG primers (moved from ig/) | moved, outputs intact |
| `pinch/pinch_common.py` + `pinch_l1..l5.ipynb` | attrib-PINCH (was ig/) | migrated, outputs intact |
| `pinch/{run,report}_pinch.py`, `_build_pinch_notebooks.py`, `_smoke_pinch.py` | PINCH harness | migrated |
| `clasp/clasp_common.py` + `clasp_l1..l5.ipynb` | contact-CLASP track | new, executed |
| `clasp/{_smoke_clasp,_build_clasp_notebooks}.py` | CLASP smoke + builder | new, verified |
| `edits/edits_common.py` + `edits_l1..l5.ipynb` | attrib-EDITS track | new, executed |
| `edits/{_smoke_edits,_build_edits_notebooks}.py` | EDITS smoke + builder | new, verified |
| `{pinch,clasp,edits}/{README,CLAUDE}.md`, `common/README.md` | per-folder docs + leakage rules | new/current |
| `_quarto.yml`, root `README.md` | site config + index | updated |
| `WRAP_0405*/0806*.md`, `experiments/{README,run_experiment.sh}` | **pre-existing** working-tree changes (NOT mine) | left untouched/unstaged |

## Where we left off
Just finished the user's request: *"prepare the quattro site. commit and push all
when done."* Built both remaining tracks (clasp, edits), executed all 10 notebooks,
wired them into Quarto, rendered the site (45 pages OK), and pushed commit `90456ce`
to `origin/main`. GitHub Pages rebuilds from the push via the Actions workflow.
The repo is in a clean, fully-pushed state for the migration + both new tracks; the
only uncommitted items are the user's **pre-existing** `WRAP_*` deletions and
`experiments/` edits, deliberately left alone.

## To resume tomorrow
**First action — bring the Quarto site up locally** (execution is OFF in
`_quarto.yml`, so this only renders committed outputs — no GPU/torch needed, just
the Quarto CLI + nbformat):
```powershell
cd C:\Users\soura\code\2026\xai-starter
$env:QUARTO_PYTHON = ".\.venv\Scripts\python.exe"   # lets Quarto read notebook outputs
quarto preview        # live-reloading local site at http://localhost:<port>
# or: quarto render   # one-shot build into _site\  (then: start _site\index.html)
```
The live site is published at **https://lampofsocrates.github.io/xai-starter/**
(rebuilt by `.github/workflows/publish.yml` on every push touching a notebook).

**If you re-execute notebooks** (to refresh outputs), two gotchas bit us today:
1. **HF SSL cert** — the shell's `SSL_CERT_FILE` points at a missing conda bundle.
   Before any ESM-2 download/run, set the venv's certifi bundle:
   ```powershell
   $env:SSL_CERT_FILE = (& .\.venv\Scripts\python.exe -c "import certifi;print(certifi.where())")
   $env:REQUESTS_CA_BUNDLE = $env:SSL_CERT_FILE
   Remove-Item Env:SSL_CERT_DIR -ErrorAction SilentlyContinue
   ```
2. **Jupyter kernel** — the global `python3` kernelspec is broken (points at a
   missing interpreter). Execute with the project kernel instead:
   `... nbconvert --execute --ExecutePreprocessor.kernel_name=xai-starter ...`
   (I also re-registered `python3` → the venv, so either works now.)

**Verify a track quickly** (CPU/GPU, ~4–9s each):
```powershell
.\.venv\Scripts\python.exe clasp\_smoke_clasp.py
.\.venv\Scripts\python.exe edits\_smoke_edits.py
.\.venv\Scripts\python.exe pinch\_smoke_pinch.py
```

**Open questions / next decisions**:
- The clasp DCA MSA is *simulated* (contact-seeded) and edits uses *ESM-2* as the
  predictor — both deliberate miniatures. Next real step per track: clasp → real
  jackhmmer/hhblits MSAs + ESM-2 650M; edits → wire `validate_against_ddg` against
  ThermoMPNN `dataset_splits/` + FireProt-HF (see `edits/CLAUDE.md` — start by
  inspecting that split's column format / multi-mutant handling).
- pinch scale-up still pending: PINDER loader (`--dataset pinder` currently raises).
- Decide whether to commit the pre-existing `WRAP_*` deletions + `experiments/`
  edits, or keep them out.

## Suggested resume prompt for a new session

> I'm continuing work in `C:\Users\soura\code\2026\xai-starter`. Read
> `WRAP_13062026_1450.md` for context. Today I want to: **[fill in — e.g. "preview
> the Quarto site", "start the edits ΔΔG validation against ThermoMPNN splits", or
> "wire the PINDER loader for pinch"]**.
