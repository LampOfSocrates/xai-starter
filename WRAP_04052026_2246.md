# Wrap-up — 04 May 2026, 22:46

## Project
`plm-starter` — a hands-on learning environment for Protein Language Models (pLMs).
Goal: be able to fine-tune small pLMs, score variants zero-shot, and swap models/tasks freely.

Working directory: `c:\Users\soura\Code\2026\plm-starter`
Python venv: `.\.venv\` (already exists)

## Today's progress
- Sketched a 5-direction plan for working with pLMs (embeddings probe → zero-shot → fine-tune → token classification → comparison harness).
- Wrote five self-contained, heavily commented lesson scripts.
- Updated `README.md` to a lesson menu + tables of swappable models & datasets.
- Bumped `requirements.txt` (`transformers>=4.46.0` for the new `eval_strategy` arg name; added `pandas`).
- **Did not run any of the scripts** — first execution will trigger model + dataset downloads.

## File inventory (current state)

| File | Purpose | Status |
|---|---|---|
| `lesson1_embeddings_probe.py` | Frozen ESM-2 + sklearn LogisticRegression on DeepSol. CPU-friendly. | New, untested |
| `lesson2_zero_shot_variants.py` | Masked-LM mutation scoring + saturation mutagenesis. No training. | New, untested |
| `lesson3_finetune_classification.py` | Full fine-tune via HF Trainer on DeepSol. Supersedes `plm_demo.py`. | New, untested |
| `lesson4_token_classification.py` | Per-residue secondary-structure prediction. Label-to-token alignment. | New, untested. Dataset column names may need adjustment — script prints `dataset.features` on startup so you can fix `SEQUENCE_KEY` / `LABEL_KEY` if needed. |
| `lesson5_model_comparison.py` | Grid: {ESM-2 8M, 35M} × {mean-pool, max-pool, cls-token}. Writes `results/lesson5_comparison.csv`. | New, untested |
| `README.md` | Lesson menu, model table, dataset sources (HF + ProteinGym + FLIP + TAPE). | Rewritten |
| `requirements.txt` | torch, transformers≥4.46, datasets, evaluate, accelerate, scikit-learn, pandas, ipywidgets | Updated |
| `plm_demo.py` | Original starter; redundant with lesson3. | Legacy — safe to delete |
| `plm_tutorial.ipynb` | Pre-existing notebook. | Untouched |

## Where we left off
Last assistant message ended with: *"I haven't executed the lessons — first run will download models/datasets, so let me know if anything errors. For Lesson 4, secondary-structure datasets vary a lot in column names…"*

So: code is written and reviewed, **next step is first execution** to validate downloads and surface any version/column-name issues.

## To resume tomorrow

**First action**: activate venv and run lesson 1 — it's the cheapest validation that the toolchain works end-to-end.

```powershell
cd c:\Users\soura\Code\2026\plm-starter
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt        # bumped transformers — make sure venv has it
python lesson1_embeddings_probe.py
```

**Then**: lesson 2 (no training — fast win) → lesson 3 (real fine-tune) → lesson 4 (will likely need column-name tweak) → lesson 5 (comparison table).

**Open questions to think about before tomorrow**:
- Which biology problem do you actually care about? (Solubility, localization, fluorescence, variant effect, secondary structure…) Knowing this lets us pick a single dataset and stop using DeepSol-as-default everywhere.
- Do you have GPU access on this machine? Affects whether 150M / 650M ESM-2 are realistic.

## Suggested resume prompt for a new session

> I'm continuing work in `c:\Users\soura\Code\2026\plm-starter`. Read `WRAP_04052026_2246.md` for context on what's already been built. Today I want to: **[your goal here — e.g. "run lesson 1 and debug any errors", or "swap lesson 3 onto the localization dataset", or "use lesson 2 against a real ProteinGym DMS file"]**.
