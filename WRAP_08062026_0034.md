# Wrap-up — 08 Jun 2026, 00:34

## Project
`C:\Users\soura\code\2026\plm-starter` — a hands-on pLM + GNN teaching repo
(protein language models + graph neural nets) of ~28 lesson notebooks plus an
"Idea-4" PINDER explainability research track. This session's goal: **wire MLflow
experiment tracking into every lesson notebook, then build a reproducible,
resource-gated CLI for running parameterised overnight experiments.**

## Today's progress
- **MLflow integration complete across all 14 training notebooks** (pLM l1/l4/l5/l7/l9/l11,
  GNN l3/l4/l7/l8/l9/l10/l11, capstone_l1) on top of the 3 done earlier (plm_l3,
  gnn_l2, capstone_l2). `gnn_l5` deliberately skipped (equivariance demo, no trained metric).
- **Verified end-to-end**: all 14 executed on GPU via a resource-gated runner; 16 MLflow
  experiments populated with params, metrics, and artifacts (plots/CSVs). Confirmed custom
  metrics + HF Trainer auto-logged metrics land correctly.
- **Built the gated papermill experiment runner** under `experiments/`: generic driver
  (validate → gate → mint ids → papermill → MLflow), an example wrapper (`exp_C_benchmark.sh`),
  and a sequential overnight batch script.
- **Reproducibility model**: every run tagged `campaign` / `exp_id` / `config_hash` (sha1 of
  sorted params — same params = same hash) / `run_uid` (unique per execution). Tags injected
  via `MLFLOW_TAG_*` env vars, auto-merged by `mlflow_utils.run()` — **no per-notebook changes**.
- **Fixed two real bugs found by smoke-testing**: (1) papermill `-p` coerced `SEEDS=[0]` to the
  string `"[0]"` → switched to a typed YAML param file; (2) Windows upper-cases env-var names so
  campaign tags landed as `CAMPAIGN` and case-sensitive MLflow filters missed them → normalised
  stripped keys to lower-case. Re-verified: `tags.campaign` filter matches, reruns share
  `config_hash` with distinct `run_uid`.
- **Created `Experiments.md`** — catalog of 6 overnight experiment ideas (A–F) with sweeps,
  expected runtimes, and fleshed-out "question it answers" + suggested batches.
- A **parallel session** committed an overlapping harness (`a88592d`, `58e51ad`) plus an Idea-4
  report generator (`ig/report_idea4.py` + demo). My commits layered cleanly on top.
- All work committed and pushed to `origin/main` (GitHub `LampOfSocrates/plm-starter`).

## File inventory (current state)
| File | Purpose | Status |
|---|---|---|
| `mlflow_utils.py` | Shared MLflow helper (`mu.run`), SQLite store, env-tag injection | modified (env-tags + lowercase fix) |
| `experiments/run_experiment.sh` | Generic driver: validate→gate→ids→papermill→MLflow | new (this session + parallel) |
| `experiments/exp_C_benchmark.sh` | Example wrapper for capstone_l2 suite | new |
| `experiments/batch_overnight.sh` | Run several experiments sequentially under one campaign | new |
| `experiments/lib/gate.py` | Block until CPU & GPU ≤70% (re-check every 5 min) | new |
| `experiments/lib/meta.py` | Mint `run_uid` + `config_hash` | new |
| `experiments/lib/mkparams.py` | `key=value` → typed YAML param file for papermill | new |
| `experiments/README.md` | Runner setup, CLI, reproducibility semantics | new |
| `experiments/runs/` | Executed notebooks + manifests | gitignored |
| `Experiments.md` | Catalog of overnight experiment ideas A–F + workflow | new |
| `_resource_check.py` / `_verify_runner.py` | Standalone gate + batch verifier | new |
| `ig/report_idea4.py` | Idea-4 report generator (+ demo) — from parallel session | new (not mine) |
| 14 lesson notebooks (plm/, gnn/, capstones/) | MLflow-wired + executed | modified |
| `mlflow.db` / `mlartifacts/` | Local MLflow store | gitignored (NOT pushed) |

## Where we left off
The runner is built, verified, and pushed (`9076fce`). The **last exchange**: I offered to
build **`mlflow_report.py <campaign>`** — the tag-level report generator that turns an overnight
campaign into `reports/report_<campaign>.md` (leaderboard + per-experiment tables + best config +
artifact links). This is the one piece still marked "to build" in `Experiments.md`. Note a
parallel session already wrote `ig/report_idea4.py` for the Idea-4 track — that's a likely
starting template to generalise, rather than writing from scratch.

No actual overnight experiment campaign has been run yet — only tiny smoke runs (since deleted
from MLflow). The catalog (A–F) is planned but unscripted beyond Experiment C's wrapper.

## To resume tomorrow
**First action**: Decide whether to (a) build `mlflow_report.py <campaign>` (generalise from
`ig/report_idea4.py`), or (b) launch the first real overnight campaign with the existing runner.

If launching a campaign:
- `experiments/exp_C_benchmark.sh --n-train 5000 --plm facebook/esm2_t12_35M_UR50D --seeds '[0,1,2,3,4]' --campaign overnight-2026-06-08`
- or `experiments/batch_overnight.sh overnight-2026-06-08` for the multi-size batch.
- Only `capstone_l2` has a papermill `parameters`-tagged cell so far; experiments A/B/D/E/F
  need their source notebooks tagged + wrapper scripts written (see `Experiments.md`).

**Open questions**:
- Build the report generator first, or run a campaign first?
- Report format — markdown (git-friendly) vs self-contained HTML vs both?
- Which experiments (A–F) to prioritise for the first real overnight run, and how big to push N.

## Suggested resume prompt for a new session

> I'm continuing work in `C:\Users\soura\code\2026\plm-starter`. Read `WRAP_08062026_0034.md`
> for context. Today I want to: **[build mlflow_report.py / launch the first overnight campaign / something else]**.
