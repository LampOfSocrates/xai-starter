.git
.claude/
__pycache__/
*.pyc
.pytest_cache/
.coverage
.venv/
venv/
.env
.DS_Store
*.ipynb_checkpoints/

# Trained model artifacts
fine_tuned_model/
*.pt
*.pth
*.safetensors

# Lesson outputs
results/

# Downloaded structures / datasets (PDB, SKEMPI) — fetched on demand
data/

# run_all_notebooks logs
run_all_*.log

# papermill-executed notebooks + per-run manifests (the MLflow store is the
# source of truth; output notebooks are reproducible from the manifest)
experiments/runs/

# MLflow experiment tracking (local store)
mlflow.db
mlartifacts/
mlruns/

# Quarto site build output (published to GitHub Pages by CI)
/_site/
/.quarto/

# Editor
.vscode/
.idea/

**/*.quarto_ipynb

# editable-install build metadata
*.egg-info/
