# Wrap-up — 08 Jun 2026, 00:01

## Project
Working dir: `C:\Users\soura\code\2026\plm-starter`. A hands-on protein-ML lesson repo (pLM / GNN / IG tracks) that now doubles as the lab bench for an **MSc research direction: "Idea 4 — cross-method xAI for PPI classification, validated against SKEMPI v2.0 alanine-scanning hotspots."** Research docs (source of truth) live in the Obsidian vault `G:\My Drive\ObsidianDB\vault\obsidian\xAI for DL in Proteins`. The hypothesis: does the **consensus** of three xAI methods (mutual attention, Integrated Gradients on GCN node embeddings, GNNExplainer) predict binding hotspots better than any single method?

## Today's progress
- **Permissions**: added vault read dir, `WebFetch(domain:*)`, `git add/commit` to `.claude/settings.local.json`.
- **README**: reframed repo around Idea 4 — research-docs index, falsifiable hypothesis, pipeline→lessons table, datasets (PINDER/SKEMPI), de-risking plan, "run responsibly" + parameterised-runs sections.
- **Validated the existing engine** `ig/idea4_common.py` (prior-session code: PDB→graphs, Struct2Graph GCN+mutual-attention, tiny trainer, 3 xAI methods, SKEMPI ground truth, AUROC) via `ig/_smoke_idea4.py` — runs on GPU in ~6 s.
- **Built 5 step-by-step notebooks** `ig_l3`–`ig_l7` (generated by `ig/_build_idea4_notebooks.py`), all executed via nbconvert with **0 errors**, gated on `_resource_check.py` (CPU+GPU ≤70%).
- **Honest finding #1 (IG saturation)**: completeness delta stays ~8.6 regardless of `n_steps` because the demo model overfits. Added a regularization sweep appendix to `ig_l5`; discovered a **cliff with no sweet spot** at 5 complexes — light reg still memorizes, heavy reg collapses to chance (acc 0.20, degenerate delta≈0). Conclusion: **scale, not hyperparameters, is the lever.** Added `dropout`/`weight_decay` to `Struct2Graph` + `train_struct2graph` (backward-compatible; functional dropout so saved weights still load).
- **Built the experiment harness** the user asked for:
  - `idea4_common.run_experiment(**hp)` → one config → flat metrics dict; pools AUROC across all 5 complexes (64 measured residues). `dataset="pinder"` raises by design.
  - `ig/run_idea4.py` → CLI (`--dropout --wd --augment --ig-steps --seed --threshold ... --tag --sweep --render`); self-gates with a 5-min wait-and-recheck loop; logs params+metrics to local MLflow.
  - `ig/report_idea4.py --tag <T>` → tag-level markdown report + chart in `ig/reports/`.
  - Demonstrated: ran `--sweep --tag report-demo`, generated `ig/reports/report-demo.md` (best method *ig* 0.526; consensus 0.504 — does NOT lead; flagged toy-scale).
- Fixed 2 bugs: empty-SKEMPI `KeyError` in `map_ddg_to_nodes`; report row-misalignment after `sort_values` (needed `reset_index`).
- `requirements.txt` += psutil/requests/certifi.

## File inventory (current state)
| File | Purpose | Status |
|---|---|---|
| `ig/idea4_common.py` | The whole pipeline engine (data, model, 3 xAI methods, SKEMPI, `run_experiment`, `benchmark_hotspots`) | modified (added dropout/wd, run_experiment, benchmark, empty-df guard) |
| `ig/ig_l3_pinder_graphs.ipynb` | complex → 2 residue graphs + interface mask | new, executed ✓ |
| `ig/ig_l4_struct2graph_train.ipynb` | Struct2Graph train; saves weights | new, executed ✓ |
| `ig/ig_l5_ig_node_embeddings.ipynb` | IG via Captum + **regularization sweep appendix** | new, executed ✓ |
| `ig/ig_l6_attention_gnnexplainer.ipynb` | attention + GNNExplainer saliencies | new, executed ✓ |
| `ig/ig_l7_skempi_consensus_benchmark.ipynb` | SKEMPI hotspots; consensus vs single AUROC | new, executed ✓ |
| `ig/_build_idea4_notebooks.py` | regenerates the 5 notebooks (source of truth for cells) | new |
| `ig/run_idea4.py` | parameterised runner → MLflow (gated) | new |
| `ig/report_idea4.py` | tag-level MLflow report generator | new |
| `ig/reports/report-demo.{md,png}` | demo report artifact | new |
| `ig/_smoke_idea4.py` | end-to-end pipeline smoke test | prior-session, passes |
| `ig/_sweep_reg.py` | throwaway reg-sweep explorer (superseded by `run_idea4.py --sweep`) | new, optional |
| `_resource_check.py` | CPU+GPU ≤70% gate (exit 0/1) | new |
| `mlflow_utils.py` | local SQLite MLflow store helper (`mu.run`) | prior-session, reused |
| `README.md` | project home; Idea 4 research track + harness docs | modified |

## Where we left off
The harness + tag-level reporting is **built and demonstrated**. The last exchange: user asked "can I run tagged experiments → tag-level report, and suggest overnight experiments with more data." I delivered the report tooling and a table of 5 overnight sweeps (`reg-sweep`, `ig-convergence`, `seed-robustness`, `threshold-sweep`, `data-volume`), but flagged that **with only 5 complexes / 64 labelled residues these characterize the plumbing + the saturation finding, NOT the consensus hypothesis** — that needs more complexes.

**I proposed (awaiting user's go-ahead): build an expanded ~25-complex SKEMPI set (within scope — just RCSB + SKEMPI CSV, no PINDER) behind a `--dataset demo-large` switch**, so an overnight seed/reg sweep would produce a report on a dataset big enough to actually read. Option (b) — the full multi-GB **PINDER loader — remains on hold until explicitly agreed**.

## To resume tomorrow
**First action**: decide between (i) build the expanded ~25-complex `demo-large` dataset (recommended, within scope), or (ii) green-light option (b) PINDER. Then kick off an overnight tagged sweep against the chosen dataset and generate the report.

- If (i): add a curated, verified list of SKEMPI complexes to `DEMO_COMPLEXES` / a new loader in `idea4_common.py`; wire `--dataset demo-large`; verify each PDB downloads + has SKEMPI rows before trusting it.
- Launch overnight, e.g.: `foreach ($s in 0..9) { .\.venv\Scripts\python.exe ig\run_idea4.py --dropout 0.3 --wd 1e-3 --seed $s --tag seed-robustness }` then `ig\report_idea4.py --tag seed-robustness`.
- Nothing has been committed yet — consider committing on a branch and adding `data/`, `*.pt`, `mlflow.db`, `mlartifacts/`, `ig/reports/`, `run_all_*.log` to `.gitignore`.

**Open questions**:
- Build the expanded `demo-large` set, or go straight to PINDER (option b)?
- Commit now (branch?) or keep iterating first?

## Suggested resume prompt for a new session

> I'm continuing work in `C:\Users\soura\code\2026\plm-starter`. Read `WRAP_08062026_0001.md` for context. Today I want to: **[e.g. "build the expanded demo-large SKEMPI complex set and run an overnight seed-robustness sweep" / "green-light the PINDER loader (option b)" / "commit everything to a branch"]**.
